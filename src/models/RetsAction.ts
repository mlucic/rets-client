/**
 * RETS action
 */
export enum RetsAction {
    /**
     * Action for get object
     */
    GetObject,
    /**
     * Action for search data, such as listings
     */
    Search,
    /**
     * Action for login
     */
    Login,
    /**
     * Action for logout
     */
    Logout
}
